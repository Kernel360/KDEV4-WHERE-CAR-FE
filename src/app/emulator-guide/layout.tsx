import SimpleLayout from '@/components/layout/SimpleLayout';

export default function EmulatorGuideLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <SimpleLayout>{children}</SimpleLayout>;
} 